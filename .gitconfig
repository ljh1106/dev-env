[user]
    name = 이지훈
    email = ljh1106@midasin.com

[core]
    editor = vim
	autocrlf = true
[color]
    status = auto
    branch = auto
    interactive = auto
    diff = auto

[alias]
    ll = log --all --decorate --oneline --graph --format=format:'%C(yellow)%h%C(reset)%C(auto)%d%C(reset) %C(normal)%s%C(reset) %C(dim white)%an%C(reset) %C(dim blue)(%ar)'
    l = log --decorate --oneline --graph --format=format:'%C(yellow)%h%C(reset)%C(auto)%d%C(reset) %C(normal)%s%C(reset) %C(dim white)%an%C(reset) %C(dim blue)(%ar)'
    st = status
    br = branch
    chk = checkout

[safe]
    directory = .
	directory = %(prefix)///wsl.localhost/Ubuntu-22.04/home/ljh1106/dev/workspace/midas-central/iam

